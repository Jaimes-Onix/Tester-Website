node_modules
dist
.DS_Store
*.local
temporary screenshots
.claude

# Secrets / API keys — NEVER commit these
.env
.env.*
!.env.example

# MCP config holds a Supabase access token — keep it out of git
.mcp.json
